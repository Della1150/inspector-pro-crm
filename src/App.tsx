import { CRM } from "@/components/atomic-crm/root/CRM";

/** Application entry point for Inspector Pro CRM. */
const App = () => <CRM title="Inspector Pro CRM" disableTelemetry />;

export default App;
